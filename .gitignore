# Secrets & runtime state — NEVER commit
.env
accounts.json
logs/
*.log

# Base de données locale (index des mails) — jamais commitée
data/
backups/
*.db
*.db-journal

# Node
# Clés privées : JAMAIS dans le dépôt, même par accident. Une clé déposée à la
# racine du projet serait embarquée par un simple `git add -A` et publiée sur
# GitHub — d'où ces motifs, posés avant que ça n'arrive (29/07).
*.key
*.pem
id_rsa*
id_ed25519*

node_modules/
dist/
npm-debug.log*

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
ux-review/
